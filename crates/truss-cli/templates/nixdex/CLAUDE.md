# CLAUDE.md

> See `AGENTS.md` for `{{ project_name }}` product rules.

## Toolchain

- Rust `stable`, edition `{{ edition }}`.
- Dev shell provides `nix-eval-jobs`.
- Verify with: `just check`, `just clippy`, `just test`.

## Commands

```bash
just check
just clippy
just test
```
