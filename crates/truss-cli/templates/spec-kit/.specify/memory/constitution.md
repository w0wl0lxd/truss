# Constitution

Working agreements for `{{ project_name }}` specs.

1. Specs live under `specs/<feature>/` only.
2. Every feature needs intent, constraints, and acceptance criteria.
3. Prefer diffs to facts over vague goals.
4. Implementation follows a spec that exists first.
5. No AI attribution in commits, PRs, or authored content.
