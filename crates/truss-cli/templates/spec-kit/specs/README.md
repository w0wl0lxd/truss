# Specs

Place feature specs under `specs/<feature>/`.

Example:

```text
specs/
  auth/
    README.md
    acceptance.md
  search/
    README.md
```

Each feature directory owns its intent, constraints, and acceptance checks.
Keep specs concrete and short. Do not dump marketing language here.
